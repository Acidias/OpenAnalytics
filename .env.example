# Required
DB_PASSWORD=changeme
JWT_SECRET=generate-a-random-secret-here  # REQUIRED - API will not start without this

# Host ports (change if defaults conflict with local services)
# DASHBOARD_PORT=3100
# API_PORT=3101
# DB_PORT=5433
# REDIS_PORT=6380

# Public tunnel URL (set this if you expose the API via Cloudflare Tunnel / ngrok)
# The tracking script tag will use this URL instead of localhost.
# TRACKER_URL=https://analytics.yourdomain.com

# CORS allowlist (comma-separated exact origins; scheme + host + optional port)
# In production this is REQUIRED and must be explicit (no wildcards).
# Development automatically allows http://localhost:${DASHBOARD_PORT:-3100}.
# Secure examples:
# CORS_ORIGIN=https://analytics.yourdomain.com
# CORS_ORIGIN=https://app.yourdomain.com,https://admin.yourdomain.com


# Reverse proxy trust configuration (recommended in production)
# TRUST_PROXY=true
# TRUSTED_PROXY_HOPS=1
# TRUSTED_PROXY_SUBNETS=127.0.0.1/8,10.0.0.0/8

# Set to false to disable new user registration
# REGISTRATION_ENABLED=true

# AI Setup Assistant - set to enable AI-powered analytics suggestions
# ANTHROPIC_API_KEY=sk-ant-...
